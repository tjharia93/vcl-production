[pre_model_sync]
# Runs before the schema is rewritten. The only thing that belongs here is work
# that must happen while a column still has its old type - everything else goes
# under [post_model_sync] below, which is where every patch in this file ran
# before the sections were introduced, and where they all still run.
production_log.patches.v8_1.normalise_carton_quantity_ordered
# Job Card Label gains the order-derived block. Both of these must run while the
# old schema is still in place: one deletes the Custom Field metadata that would
# otherwise collide with the new DocFields (without touching the columns those
# fields own), the other censuses quantity_ordered in the last moment before it
# stops being an Int. Their post-sync counterpart is verify_label_order_migration.
production_log.patches.v8_2.retire_label_order_custom_fields
production_log.patches.v8_2.audit_label_quantity_ordered
# Normalise part colours to the Colour Item Attribute's spellings BEFORE the same
# release turns colour into a Select in colour_of_parts.json. Pre-sync because the
# doctype change lands at model sync: rewriting the rows first is what stops every
# spec holding "WHITE" becoming unsaveable, the same failure v9_5 had to undo on
# Workstation.
production_log.patches.v9_6.normalise_part_colours

[post_model_sync]
production_log.patches.v1_0.setup_roles_and_permissions
production_log.patches.v1_0.remove_job_card_production_entry
production_log.patches.v1_0.rename_workspace_to_vcl_production
production_log.patches.v1_0.remove_production_execution_layer
production_log.patches.v2_0.setup_ppc_custom_fields
production_log.patches.v2_0.add_carton_sfk_flag
production_log.patches.v3_0.remove_execution_layer
production_log.patches.v4_0.seed_pantone_palette
production_log.patches.v4_0.migrate_colour_fields
production_log.patches.v4_0.set_default_print_formats
production_log.patches.v4_0.backfill_job_status
production_log.patches.v4_1.migrate_label_status_to_in_progress
production_log.patches.v4_1.init_job_card_status
production_log.patches.patch_v5_0
production_log.patches.patch_v5_1
production_log.patches.patch_v5_2
production_log.patches.patch_v5_3
production_log.patches.patch_v5_4
production_log.patches.patch_v5_5
production_log.patches.v6_0.setup_ppc_v2_roles
production_log.patches.v6_0.setup_ppc_jc_custom_fields
production_log.patches.v7_0.add_print_type
production_log.patches.v7_0.backfill_print_type
production_log.patches.v7_0.add_traveller_run_resource_consumption
production_log.patches.v7_0.expand_job_status_whitelist
production_log.patches.v7_1.add_carton_workstations
production_log.patches.v7_2.add_readiness_status
production_log.patches.v7_3.finish_carton_workstations
production_log.patches.v7_4.add_process_number_routes
production_log.patches.v7_5.notebook_32_carton_review
production_log.patches.v7_6.add_cp_production_stages
production_log.patches.v7_6.migrate_job_status_in_progress
production_log.patches.v8_0.add_cps_control_custom_fields
production_log.patches.v8_0.add_cps_iteration_two_fields
production_log.patches.v8_2.stamp_legacy_label_order_references
production_log.patches.v8_2.verify_label_order_migration
# Computer Paper specification capture: the numbering-confirmation flag, its
# notes, and the artwork pair. Additive Custom Fields only, no backfill - every
# existing record legitimately has numbering_confirmed unset.
production_log.patches.v8_3.add_cps_numbering_artwork_fields
# Computer Paper carton-weight calculation: the structured inputs (finished size,
# sets per carton, carton tare, optional printing allowance) and the read-only
# derived results (total GSM, per-set weight, net and gross weight per carton).
# Additive Custom Fields only, no backfill - legacy records compute nothing until
# their inputs are entered, and the gross mirrors into standard_weight_per_carton.
production_log.patches.v8_3.add_cp_weight_fields
# The finished size is entered in inches - the unit computer paper is actually
# ordered in - and the millimetre fields become read-only, derived (x 25.4) and
# relabelled "Converted". The millimetre columns keep their meaning, because the
# specs submitted since April store real millimetres in them. Backfills the inch
# entry from every stored millimetre size, with update_modified=False so no order
# snapshot reads as stale.
production_log.patches.v8_4.add_cp_inch_dimensions
# Monobox joins the product lines: a Customer Product Specification type, a die
# register of its own (Dies is the flexo label register and describes a rotary
# tool), the job card, and the traveller. This patch only does what a migrate
# cannot - the default print format Property Setter and the workspace links.
production_log.patches.v9_0.setup_monobox
# The die register is named for the tool, not the product: the same flatbed
# forme cuts a monobox, a die-cut carton and a 2-ply cup sleeve. Must run before
# the model sync, which every patch in this app does - patches.txt carries no
# [post_model_sync] header, so Frappe parses it in old format as all-pre.
production_log.patches.v9_1.rename_monobox_die_to_flatbed
# The artwork tracker link, the deferral flag, and a Desk control for the
# Numbering question. v8_3 enforced both rules on a form that could not satisfy
# either: numbering_confirmed is hidden and Desk had no way to answer, and the
# artwork it demanded already lives in the Design and Artwork Tracker. Additive
# throughout - no backfill, and the existing estate is untouched.
production_log.patches.v9_2.add_cps_artwork_tracker_and_numbering_answer
# CPT-SPEC-00053 carried print_type="Plain" while linked to a Pre-Printed Item
# and ticking all four CMYK inks - a contradiction neither the colour rules nor
# the new Item/Print Type rule caught, because both are transition rules and
# the record predates either. One-record correction, checked against the whole
# linked estate first; see the patch docstring for why "Printed" is the right
# answer, not "Plain".
production_log.patches.v9_3.fix_cpt_spec_00053_print_type
# Seven more Computer Paper specifications carry the same defect as
# CPT-SPEC-00053 - print_type="Plain" while recording real CMYK ink - missed
# by v9_3 because none of them has a linked_item for the new rule to check
# against. The always-on colour rule (Plain cannot carry ink) is the signal
# instead. Checked against the full live Plain population first: 11 total, 2
# genuinely Plain and untouched, 1 cancelled and untouched, these 7 corrected.
production_log.patches.v9_4.fix_plain_computer_paper_with_ink
# patch_v5_5 dropped "All" from the Workstation Product Line Select but never
# re-tagged the records still holding it, so nine live Workstations - both
# collators among them - could not be saved at all. Restores the option (and
# with it the field's own default, which still pointed at "All"). Option list
# only: nothing is re-tagged, because several of those machines genuinely
# serve more than one line. Mirrored in fixtures/custom_field.json, which is
# what a deploy actually applies.
production_log.patches.v9_5.restore_all_product_line_option
# The BOM generated from a specification. Not a convenience: several specs
# share one linked_item with different colour recipes, so is_default on the
# BOM cannot say which recipe belongs to which job.
production_log.patches.v9_6.add_cps_linked_bom
