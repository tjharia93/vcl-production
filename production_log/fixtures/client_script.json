[
 {
  "doctype": "Client Script",
  "dt": "Customer Product Specification",
  "enabled": 1,
  "name": "CPS Price Approval Toolbar",
  "script": "frappe.ui.form.on(\"Customer Product Specification\", {\n refresh(frm) {\n  if (frm.is_new() || !frappe.user.has_role(\"Sales Master Manager\")) return;\n  const decide = (action) => {\n   if (frm.is_dirty()) { frappe.msgprint(__(\"Save the specification before approving or rejecting a price.\")); return; }\n   const grid = frm.fields_dict.pricing && frm.fields_dict.pricing.grid;\n   const selected = grid ? grid.get_selected_children() : [];\n   const drafts = (frm.doc.pricing || []).filter(row => (row.approval_status || \"Draft\") === \"Draft\");\n   const row = selected.length === 1 ? selected[0] : (drafts.length === 1 ? drafts[0] : null);\n   if (!row) { frappe.msgprint(__(\"Tick one Draft price row first.\")); return; }\n   const approving = action === \"approve\";\n   frappe.prompt([{fieldname:\"notes\",fieldtype:\"Small Text\",label:__(\"Approval Notes\"),reqd:!approving}], values => frappe.call({\n    method:\"production_log.job_card_tracking.cps_pricing.approve_cps_price\",\n    args:{cps:frm.doc.name,row:row.name,action:action,notes:values.notes},\n    freeze:true,freeze_message:__(\"Recording the decision...\"),\n    callback(r) { if (r.message) { frappe.show_alert({message:__(\"Price row is now {0}.\",[r.message.approval_status]),indicator:approving?\"green\":\"red\"}); frm.reload_doc(); } }\n   }), approving?__(\"Approve Price\"):__(\"Reject Price\"), approving?__(\"Approve\"):__(\"Reject\"));\n  };\n  frm.add_custom_button(__(\"Approve Price\"),()=>decide(\"approve\"),__(\"Pricing\"));\n  frm.add_custom_button(__(\"Reject Price\"),()=>decide(\"reject\"),__(\"Pricing\"));\n }\n});",
  "view": "Form"
 },
 {
  "doctype": "Client Script",
  "dt": "Customer Product Specification",
  "enabled": 1,
  "name": "CPS \u2014 Finished Size in Inches",
  "script": "// \u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\n// CPS \u2014 Finished Size in Inches\n//\n// Computer Paper is specified, quoted and ordered in inches \u2014 \"9.5 x 8\",\n// \"9.5 x 11\". The weight arithmetic works in millimetres, because\n// `cps_cp_weight.area_m2` is mm\u00b2 \u2192 m\u00b2. So inches are the entry and\n// millimetres are derived: mm = in \u00d7 25.4, to three decimals.\n//\n// The millimetre fields are read-only on the form but are still the ONLY\n// thing the weight is computed from (`WIDTH_FIELD` / `LENGTH_FIELD` in\n// production_log/job_card_tracking/cps_cp_weight.py). This script keeps them\n// in step while typing; the DocType controller re-derives them on save, so a\n// REST or Data Import caller that never runs this script lands on the same\n// numbers.\n//\n// The conversion deliberately runs on a SUBMITTED specification too. The whole\n// weight section is allow_on_submit \u2014 a spec written before the weights existed\n// is completed in place rather than cancelled, per the revise-in-place rule \u2014\n// and `before_update_after_submit` recomputes the weights from the millimetres.\n// If this script stopped at docstatus 0, typing inches on a submitted spec\n// would leave the millimetres at zero and compute a carton that weighs nothing.\n//\n// The inch back-fill is the one thing still restricted to drafts: writing to a\n// submitted document on load would mark it Not Saved before the reader had\n// touched anything. On a submitted spec the inches get typed, which is the\n// explicit act that ought to be required there anyway.\n// \u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\n\n(function () {\n\tif (window.__VCL_CPS_INCHES__) return;\n\twindow.__VCL_CPS_INCHES__ = true;\n\n\tconst MM_PER_INCH = 25.4;\n\tconst PAIRS = [\n\t\t{ inches: \"finished_width_in\", mm: \"finished_width_mm\" },\n\t\t{ inches: \"finished_length_in\", mm: \"finished_length_mm\" },\n\t];\n\n\t// Three decimals of a millimetre is 1 \u00b5m \u2014 far finer than a guillotine, and\n\t// enough that in \u2192 mm \u2192 in returns the number that was typed.\n\tconst round3 = (n) => Math.round(n * 1000) / 1000;\n\n\tfunction is_cp(frm) {\n\t\treturn frm.doc.product_type === \"Computer Paper\";\n\t}\n\n\tfunction push_mm(frm, pair) {\n\t\tif (!is_cp(frm) || frm.doc.docstatus === 2) return;\n\t\tconst inches = flt(frm.doc[pair.inches]);\n\t\t// Clearing the inches clears the millimetres: a stale converted value\n\t\t// under an empty input reads as a size somebody entered.\n\t\tconst mm = inches > 0 ? round3(inches * MM_PER_INCH) : 0;\n\t\tif (flt(frm.doc[pair.mm]) !== mm) frm.set_value(pair.mm, mm);\n\t}\n\n\tfunction backfill_inches(frm) {\n\t\tif (!is_cp(frm) || frm.doc.docstatus !== 0) return;\n\t\tPAIRS.forEach((pair) => {\n\t\t\tconst mm = flt(frm.doc[pair.mm]);\n\t\t\tif (mm > 0 && !flt(frm.doc[pair.inches])) {\n\t\t\t\tfrm.set_value(pair.inches, round3(mm / MM_PER_INCH));\n\t\t\t}\n\t\t});\n\t}\n\n\tconst handlers = {\n\t\trefresh(frm) {\n\t\t\tbackfill_inches(frm);\n\t\t},\n\t\tproduct_type(frm) {\n\t\t\tbackfill_inches(frm);\n\t\t},\n\t};\n\tPAIRS.forEach((pair) => {\n\t\thandlers[pair.inches] = (frm) => push_mm(frm, pair);\n\t});\n\n\tfrappe.ui.form.on(\"Customer Product Specification\", handlers);\n})();\n",
  "view": "Form"
 },
 {
  "doctype": "Client Script",
  "dt": "Customer Product Specification",
  "enabled": 1,
  "name": "CPS \u2014 Revise Customer Spec Button",
  "script": "frappe.ui.form.on(\"Customer Product Specification\", {\n    refresh(frm) {\n        if (frm.doc.docstatus !== 1) return;\n\n        frm.add_custom_button(\"Revise Customer Spec\", () => open_revise_dialog(frm)).addClass(\"btn-primary\");\n\n        frm.add_custom_button(\"Change history\", () => {\n            frappe.set_route(\"List\", \"Version\", {\n                ref_doctype: \"Customer Product Specification\",\n                docname: frm.doc.name,\n            });\n        });\n    },\n});\n\nfunction open_revise_dialog(frm) {\n    const is_cp = frm.doc.product_type === \"Computer Paper\";\n    const rows = (frm.doc.colour_of_parts || []).map((r) => ({\n        part_number: r.part_number,\n        paper_type: r.paper_type,\n        gsm: r.gsm,\n        colour: r.colour,\n    }));\n\n    const fields = [\n        {\n            fieldtype: \"HTML\",\n            fieldname: \"blurb\",\n            options:\n                '<div class=\"text-muted small\" style=\"margin-bottom:8px\">Revises <b>' +\n                frappe.utils.escape_html(frm.doc.name) +\n                '</b> in place. The spec keeps its name, job cards already raised keep the old values on their travellers, and the change is written to the Version log plus Revision Notes. Nothing is cancelled or amended.</div>',\n        },\n        {\n            fieldtype: \"Small Text\",\n            fieldname: \"reason\",\n            label: \"Reason for revision\",\n            reqd: 1,\n            description: \"Why is the spec changing? e.g. customer LPO ref, email, phone confirmation.\",\n        },\n    ];\n\n    if (is_cp) {\n        fields.push({\n            fieldtype: \"Table\",\n            fieldname: \"parts\",\n            label: \"Colour of Parts\",\n            cannot_add_rows: true,\n            cannot_delete_rows: true,\n            in_place_edit: true,\n            data: rows,\n            get_data: () => rows,\n            description:\n                \"Part 1 must be CB 55gsm, middle parts CFB 50gsm, last part CF 55gsm. Parts cannot be added or removed here.\",\n            fields: [\n                { fieldname: \"part_number\", fieldtype: \"Int\", label: \"Part\", in_list_view: 1, read_only: 1, columns: 1 },\n                { fieldname: \"paper_type\", fieldtype: \"Select\", label: \"Paper\", options: \"CB\\nCFB\\nCF\\n60 GSM Bond\\n70 GSM Bond\", in_list_view: 1, columns: 3 },\n                { fieldname: \"gsm\", fieldtype: \"Int\", label: \"GSM\", in_list_view: 1, columns: 2 },\n                { fieldname: \"colour\", fieldtype: \"Data\", label: \"Colour\", in_list_view: 1, reqd: 1, columns: 4 },\n            ],\n        });\n    }\n\n    const d = new frappe.ui.Dialog({\n        title: \"Revise Customer Spec\",\n        size: \"large\",\n        fields: fields,\n        primary_action_label: \"Revise\",\n        primary_action(values) {\n            const args = { spec: frm.doc.name, reason: (values.reason || \"\").trim() };\n            if (is_cp) {\n                const parts = (d.fields_dict.parts.get_value() || []).map((r) => ({\n                    part_number: r.part_number,\n                    paper_type: r.paper_type,\n                    gsm: r.gsm,\n                    colour: r.colour,\n                }));\n                args.parts = JSON.stringify(parts);\n            }\n            frappe.call({\n                method: \"cps_revise\",\n                args: args,\n                freeze: true,\n                freeze_message: \"Recording revision\u2026\",\n            }).then((r) => {\n                const m = (r && r.message) || {};\n                if (!m.ok) return;\n                d.hide();\n                frm.reload_doc();\n                frappe.msgprint({\n                    title: \"Revision recorded\",\n                    indicator: \"green\",\n                    message:\n                        m.changes && m.changes.length\n                            ? \"<ul><li>\" + m.changes.map(frappe.utils.escape_html).join(\"</li><li>\") + \"</li></ul>\"\n                            : \"Note recorded; no field values changed.\",\n                });\n            });\n        },\n    });\n    d.show();\n}\n",
  "view": "Form"
 }
]