[build-system]
requires = ["flit_core >=3.4,<4"]
build-backend = "flit_core.buildapi"

[project]
name = "vcl_production"
authors = [
    {name = "VCL", email = "admin@vcl.co.ke"}
]
description = "VCL Production Lite - structured daily production entry and reporting for Vimit Converters Ltd"
readme = "README.md"
requires-python = ">=3.10"
license = {text = "MIT"}
dependencies = []

dynamic = ["version"]

[project.urls]
Repository = "https://github.com/tjharia93/vcl-production"
